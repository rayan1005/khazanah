rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    
    function isAuthenticated() {
      return request.auth != null;
    }
    
    function isOwner(userId) {
      return request.auth.uid == userId;
    }
    
    function isImage() {
      return request.resource.contentType.matches('image/.*');
    }
    
    function isUnder5MB() {
      return request.resource.size < 5 * 1024 * 1024;
    }
    
    // Post images
    match /posts/{postId}/{allPaths=**} {
      allow read: if true;
      allow write: if isAuthenticated() && isImage() && isUnder5MB();
    }
    
    // User avatars
    match /users/{userId}/{allPaths=**} {
      allow read: if true;
      allow write: if isOwner(userId) && isImage() && isUnder5MB();
    }
    
    // Brand images (admin checked in app)
    match /brands/{brandId}/{allPaths=**} {
      allow read: if true;
      allow write: if isAuthenticated() && isImage() && isUnder5MB();
    }
    
    // Banner images (admin checked in app)
    match /banners/{bannerId}/{allPaths=**} {
      allow read: if true;
      allow write: if isAuthenticated() && isImage() && isUnder5MB();
    }
    
    // Section images (admin checked in app)
    match /sections/{sectionId}/{allPaths=**} {
      allow read: if true;
      allow write: if isAuthenticated() && isImage() && isUnder5MB();
    }
    
    // Category images (admin checked in app)
    match /categories/{categoryId}/{allPaths=**} {
      allow read: if true;
      allow write: if isAuthenticated() && isImage() && isUnder5MB();
    }
    
    // Boutique images (logo, cover, certificates)
    match /boutiques/{userId}/{allPaths=**} {
      allow read: if true;
      allow write: if isAuthenticated() && isUnder5MB();
    }
  }
}
